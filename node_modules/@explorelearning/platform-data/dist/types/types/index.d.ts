export declare type valueOf<T> = T[keyof T];
export declare type UserAction = {
    userId: number;
    timestamp: string;
};
export declare type RawUserAction = {
    loginId: number;
    timestamp: string;
};
