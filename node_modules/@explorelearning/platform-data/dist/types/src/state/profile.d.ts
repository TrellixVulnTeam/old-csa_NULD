declare const _default: ({ apis }: {
    apis: any;
}) => {
    namespaced: boolean;
    state: {
        profile: {};
    };
    mutations: {
        SET_PROFILE(state: any, profile: any): void;
    };
    actions: {
        fetchProfile({ commit }: {
            commit: any;
        }): Promise<void>;
    };
    getters: {
        isHomeUser(state: any): boolean;
        isStudent(state: any): boolean;
        isTeacher(state: any): boolean;
        isParent(state: any): boolean;
        isAdmin(state: any): boolean;
        isEmployee(state: any): boolean;
        isDeveloper(state: any): boolean;
        profile(state: any): any;
    };
};
/**
 * Module providing action for fetching the user's profile and state for the profile data, as well as helpful getters for checking the user's role
 */
export default _default;
