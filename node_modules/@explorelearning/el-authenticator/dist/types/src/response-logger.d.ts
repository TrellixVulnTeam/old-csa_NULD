import { AxiosResponse } from 'axios';
declare type ElXClaims = {
    sessionID: number | string;
};
declare type ElHttpResponseConfig = {
    url?: string;
};
declare type ElHttpResponseBody = {
    error: string;
};
declare type ElHttpResponse = {
    status: number;
    config?: ElHttpResponseConfig;
    body?: ElHttpResponseBody;
};
export default class ResponseLogger {
    private logEndpoint;
    constructor(logEndpoint: string);
    private getFormattedTime;
    log(response: ElHttpResponse, xclaims: ElXClaims, error: string): Promise<AxiosResponse<any>>;
}
export {};
